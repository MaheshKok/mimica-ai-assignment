"""Mock Workflow Services API.

Standalone FastAPI app that mimics the Workflow Services contract
(``/projects/{projectId}/stream`` NDJSON stream and ``/qa/answer``) so
the service can be exercised end-to-end without any paid dependency.
"""
